# CRM Weekly Report Setup

This document explains how to set up Celery with Celery Beat to generate weekly CRM reports and log them.

## 1. Install Dependencies

Make sure Python and pip are installed, then install required packages:

```bash
pip install -r requirements.txt
